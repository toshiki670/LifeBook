query GetAll {
  library {
    books {
      id
      title
      author
      description
      publishedYear
    }
  }
}

query GetOne($id: Int!) {
  library {
    book(id: $id) {
      id
      title
      author
      description
      publishedYear
    }
  }
}
