# すべての本を取得
query GetBooks {
  library {
    books {
      id
      title
      author
      description
      publishedYear
    }
  }
}

# IDで本を取得
query GetBook($id: Int!) {
  library {
    book(id: $id) {
      id
      title
      author
      description
      publishedYear
    }
  }
}
