/**
 * Test helper functions for CI verification
 */

/**
 * Formats a greeting message
 */
export function formatGreeting(name: string): string {
  return `Hello, ${name}!`
}

/**
 * Adds two numbers together
 */
export function add(a: number, b: number): number {
  return a + b
}
