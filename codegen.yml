overwrite: true
schema: "schema.graphql"
documents: "src/**/*.graphql"  # documentsを有効化
generates:
  src/generated/:
    preset: "client"
    config:
      useTypeImports: true
      skipTypename: false
      documentMode: "documentNode"
