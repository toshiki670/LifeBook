overwrite: true
schema: "schema.graphql"
documents: "src/**/*.graphql"
generates:
  src/generated/:
    preset: "client"
    config:
      useTypeImports: true
      skipTypename: false
      documentMode: "documentNode"

